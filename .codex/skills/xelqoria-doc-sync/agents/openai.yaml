interface:
  display_name: "Xelqoria ドキュメント同期"
  short_description: "PR差分に基づいて必要なドキュメントのみ更新"
  default_prompt: "$xelqoria-doc-sync を使って、このPR差分を確認し、最も関連する docs 配下のドキュメント1つだけを最小限更新してください。PRに含まれない内容は変更せず、推測で追記しません。不明な場合は更新しません。"

policy:
  allow_implicit_invocation: true